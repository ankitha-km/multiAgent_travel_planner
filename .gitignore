# WANDR — files that should never go to GitHub

# API keys — never share this
config.js

# Windows junk files
Thumbs.db
desktop.ini

# Mac junk files
.DS_Store

trip.html
trip_deploy.html

# API keys — never push
config.js

# Android build files — too large for GitHub
android/
www/

# Node modules — too large, auto-generated
node_modules/

# Windows junk
Thumbs.db
desktop.ini

# Mac junk
.DS_Store